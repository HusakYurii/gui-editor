export type TContainerData = {
    positionX: number;
    positionY: number;
    scaleX: number;
    scaleY: number;
    rotation: number;
    alpha: number;
}

export type TContainersDataMap = Record<string, TContainerData>;